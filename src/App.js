function App() {
  const num1 = Math.ceil(Math.random() * 6);
  const num2 = Math.ceil(Math.random() * 6);

  return <div></div>;

  // 두 수를 출력하고
  // 두 수가 같으면
  // "당첨!!"
  // 두 수가 다르면
  // "다시 던져 보세요"
}

export default App;
