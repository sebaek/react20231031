import React, { useState } from "react";

function App(props) {
  const [number, setNumber] = useState(0);

  if (number > 5) {
    const [text, setText] = useState("");
  }
  return <div></div>;
}

export default App;
